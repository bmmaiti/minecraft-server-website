# 每日自动下载友链 Logo 任务

## 使用方法

### 方法一：双击运行（手动）
双击 `auto-download-logos.bat` 文件即可运行。

### 方法二：设置定时任务（推荐）

#### Windows 任务计划程序设置：

1. **打开任务计划程序**
   - 按 `Win + R` 键
   - 输入 `taskschd.msc` 并按回车

2. **创建基本任务**
   - 点击右侧的"创建基本任务"
   - 输入名称：`每日自动下载友链 Logo`
   - 输入描述：`每天自动下载所有友链的 logo 到本地缓存`

3. **设置触发器**
   - 选择"每天"
   - 设置开始时间（推荐凌晨 2:00-5:00，网站访问量少）
   - 点击"下一步"

4. **设置操作**
   - 选择"启动程序"
   - 程序/脚本：浏览到 `auto-download-logos.bat`
   - 点击"下一步"

5. **完成设置**
   - 点击"完成"

## 工作原理

1. **每天定时运行**：脚本会自动从友链网站获取 logo
2. **下载到本地**：logo 保存在 `/logos/` 目录
3. **保存缓存信息**：每个 logo 对应一个 `.json` 文件，记录下载时间和大小
4. **网站使用缓存**：用户访问网站时，直接使用本地缓存的 logo，无需重新获取

## 缓存文件结构

```
/logos/
├── domain.com.png          # logo 文件
├── domain.com.json         # 缓存信息（下载时间、大小等）
├── another-domain.webp
└── another-domain.json
```

## 缓存信息示例

```json
{
  "downloadedAt": "2024-03-02T02:00:00.000Z",
  "size": 12345,
  "url": "https://example.com/logo.png"
}
```

## 手动更新缓存

如果需要手动更新所有 logo，可以：
- 双击运行 `auto-download-logos.bat`
- 或使用命令：`npm run download-logos`

## 注意事项

- 确保服务器 24 小时运行，定时任务才能正常执行
- 如果网站访问量大，建议在凌晨低峰期运行
- 定期检查 `/logos/` 目录，确保缓存正常更新
