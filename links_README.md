# 友情链接页面使用说明

## 功能介绍
这是一个简洁美观的友情链接页面，具有以下功能：
- 自动获取网站Logo（使用国内可访问的favicon服务）
- 响应式网格布局，适配各种屏幕尺寸
- 美观的卡片式设计，带悬停动画效果
- 支持添加网站名称、URL和描述信息（通过直接编辑JSON文件）

## 如何添加新的友链

要添加新的友链，请编辑 `links.json` 文件，在数组中添加一个新的对象，格式如下：

```json
{
  "id": 5,
  "name": "网站名称",
  "url": "https://www.example.com",
  "description": "网站描述"
}
```

### 参数说明
- `id`: 友链的唯一标识符（数字），请确保每个友链的ID都不相同
- `name`: 网站名称（必填）
- `url`: 网站完整地址（必填，包含http://或https://）
- `description`: 网站描述（可选）

### 示例
```json
[
  {
    "id": 1,
    "name": "GitHub",
    "url": "https://www.github.com",
    "description": "全球最大的代码托管平台和开源社区"
  },
  {
    "id": 2,
    "name": "Stack Overflow",
    "url": "https://stackoverflow.com",
    "description": "程序员必备的问答社区"
  }
]
```

## 自定义样式
如果需要修改页面样式，可以编辑 `links.html` 文件中的CSS部分，其中包括：
- 页面背景样式
- 卡片布局和阴影效果
- 响应式网格设置
- 悬停动画效果

## 技术细节
- 使用国内可访问的favicon服务获取网站图标
- 如果服务失败，则显示默认的SVG图标
- 采用现代CSS Grid布局实现响应式设计
- 数据存储在独立的JSON文件中，便于维护
- 通过直接编辑JSON文件来管理友链，无需前端表单

## 注意事项
- 添加新友链后，刷新页面即可看到更新
- 确保URL格式正确（包含http://或https://）
- 网站Logo会自动从网站获取，如果获取失败会显示默认图标
- 请确保服务器支持对JSON文件的访问